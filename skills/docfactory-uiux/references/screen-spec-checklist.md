# Screen Spec Checklist

Every MVP screen must specify:

- Purpose (1 sentence)
- Primary actions (max 3)
- Components used (from inventory)
- States:
  - Loading
  - Empty
  - Error (with retry)
- Edge cases (permissions, auth expired, offline)
- Analytics events (using glossary naming)
- Accessibility notes (labels, tap targets)
