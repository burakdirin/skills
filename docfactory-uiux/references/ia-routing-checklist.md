# IA & Routing Checklist (Expo Router)

- Use route groups with parentheses: `(auth)`, `(tabs)`
- Keep route filenames in kebab-case
- Keep non-navigation components outside the `app/` directory
- Ensure the PRD flows map to routes without adding new MVP screens
- Identify which screens are modal vs stacked vs tab
